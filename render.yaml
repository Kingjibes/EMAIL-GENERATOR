services:
  - type: web
    name: ciphertech-email-generator
    env: node
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: PORT
        value: 3000
    staticSite: false